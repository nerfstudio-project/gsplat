# 0000 — Initialization

Date: 2026-04-23
Author: scaffolding agent

## What landed

- Created branch `vnath_gsharp` off `nv/main` in `gsplat/`.
- Created `planning/` directory with:
  - `gsharp_gsplat_plan.md` — human-readable plan
  - `gsharp_gsplat_plan.html` — shareable single-file HTML
  - `dev_updates/` — this folder, for append-only dev log
- Scaffolded empty stub modules and tests inside `gsplat/` at the target paths so PR reviewers can see the architecture without any implementation noise.
- Proposal doc checked in at `gsplat/docs/source/proposals/gsharp_v0_2_port.rst`.

## How to add a dev update

Append a new file `NNNN_<slug>.md` (monotonically increasing four-digit index). Each entry should contain:

- Date + author
- What was worked on
- What's now green in tests
- Any follow-ups or blockers

## Next up

See todos on the pinned plan. Implementation starts once the scaffolding PR is reviewed and the architecture is signed off.
